server:
  port: 1106

spring:
  datasource:
    username: root
    password: Tiengie987@
    url: jdbc:mysql://localhost:3306/lakeSide_hotel_db?allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
  servlet:
    multipart:
    max-file-size: 5MB
    max-request-size: 5MB
    file-size-threshold: 2KB
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher